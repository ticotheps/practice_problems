import unittest
from depth_first_search import depthFirstSearch

class Test(unittest.TestCase):
    def test_depthFirstSearch(self):
        self.assertEqual(depthFirstSearch(), )
        self.assertEqual(depthFirstSearch(), )
        self.assertEqual(depthFirstSearch(), )
        self.assertEqual(depthFirstSearch(), )
        self.assertEqual(depthFirstSearch(), )
        self.assertEqual(depthFirstSearch(), )

if __name__ == '__main__':
    unittest.main()

