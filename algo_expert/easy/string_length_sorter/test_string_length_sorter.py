import unittest
from string_length_sorter import string_length_sorter

class Test(unittest.TestCase):
    def test_string_length_sorter(self):
        self.assertEqual(string_length_sorter(), )
        self.assertEqual(string_length_sorter(), )
        self.assertEqual(string_length_sorter(), )
        self.assertEqual(string_length_sorter(), )
        self.assertEqual(string_length_sorter(), )
        
if __name__ == '__main__':
    unittest.main()